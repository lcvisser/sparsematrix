name: CMake

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install toolchain
      run: sudo apt-get install -y lcov

    - name: Configure CMake
      run: cmake --preset default-configure-coverage

    - name: Build
      run: cmake --build --preset default-build-coverage

    - name: Test
      working-directory: ${{github.workspace}}/build
      run: ctest

    - name: Collect coverage
      working-directory:
      run: |
        lcov --capture --directory testbin/ --include "$SRCDIR/sparsematrix.h" --gcov-tool /opt/local/bin/gcov --output-file coverage.info
        lcov --list coverage.info
